!(function (e) {
	if ("querySelectorAll" in e) {
		var t,
			r = new Date(),
			l = r.getUTCMonth() + 1,
			s = r.getUTCDate(),
			n = e.querySelector("#past-events + .listing--events"),
			a = e.querySelectorAll(".event--future"),
			i = 0,
			u = a.length,
			o = /-/g;
		for (
			r =
				"" +
				r.getUTCFullYear() +
				(l < 10 ? "0" + l : l) +
				(s < 10 ? "0" + s : s);
			i < u;
		)
			(t = a[i++]).dataset.date.replace(o, "") < r &&
				(t.classList.remove("event--future"),
				t.classList.add("event--past"),
				n.insertBefore(t, n.firstChild));
	}
})(this.document);
